import { PasswordSettings } from "./PasswordSettings";

export const Settings = () => {
    return (
        <div className="settings-container">
            <span>Settings</span>
            <PasswordSettings />
        </div>
    )
}
